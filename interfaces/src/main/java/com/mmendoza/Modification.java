package com.mmendoza;

public interface Modification {

    // Declaration of the increment method; it's public and abstract by default in an interface
    // This method will take an Integer as an argument and doesn't return anything (void)
    void increment(Integer a);
}
